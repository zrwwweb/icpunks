#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../group-css-media-queries/bin/group-css-media-queries" "$@"
  ret=$?
else 
  node  "$basedir/../group-css-media-queries/bin/group-css-media-queries" "$@"
  ret=$?
fi
exit $ret
